// Disable SSR for Capacitor SPA mode - client-side rendering only
export const ssr = false;

// Don't prerender dynamic routes - use client-side routing
export const prerender = false;

// Enable trailing slash handling
export const trailingSlash = 'never';
